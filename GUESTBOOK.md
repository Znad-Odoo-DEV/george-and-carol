# دفتر المباركات — التشغيل

الواجهة جاهزة ومختبَرة بالكامل. ينقصها سطر واحد: عنوان مكان تُحفظ فيه المباركات.

## لماذا يحتاج إعداداً أصلاً

الدعوة صفحة ثابتة على GitHub Pages — لا خادم خلفها. المباركة التي يكتبها ضيف
على هاتفه لا بدّ أن تُحفظ في مكانٍ مشترك، وإلا فلن يراها أحد سواه. أيّ حلّ
يخزّنها داخل المتصفّح وحده يعني أن كل زائر يرى كلماته هو فقط ويظنّ أن الجميع
يرونها — وهذا ما لن نضعه على دعوة زفاف حقيقية.

لذلك: **جدول Google Sheets يخصّك أنت**، مع سكربت صغير يقرأ منه ويكتب فيه.
مجاني بلا حدود عملية، بلا بطاقة، والبيانات في حسابك وحدك، وحذف صفٍّ من الجدول
يحذف المباركة من الصفحة.

**الضيف لا يسجّل دخولاً ولا يترك بريداً ولا رقماً** — اسمٌ وكلمة، لا أكثر.

---

## الخطوات — دقيقتان

### ١ · أنشئ الجدول والسكربت

1. افتح <https://sheets.new> — يُنشأ جدول جديد. سمِّه مثلاً `مباركات جورج وكارول`.
2. من القائمة: **الإضافات ← Apps Script** (`Extensions → Apps Script`).
3. امسح كل ما في المحرّر، والصق محتوى الملف [`guestbook.gs`](guestbook.gs) كاملاً.
4. احفظ (💾).

### ٢ · انشره كتطبيق ويب

1. من الأعلى يميناً: **نشر ← عملية نشر جديدة** (`Deploy → New deployment`).
2. اضغط ⚙️ بجانب `Select type` واختر **تطبيق ويب** (`Web app`).
3. اضبط الحقلين:
   - **التنفيذ باسم** (`Execute as`) → **أنا** (`Me`)
   - **من له حق الوصول** (`Who has access`) → **أي شخص** (`Anyone`)
4. **نشر** ← ستطلب منك جوجل السماح للسكربت بالوصول إلى جدولك؛ وافق.
   (ستمرّ بشاشة تحذير لأن السكربت غير موثّق لدى جوجل — اضغط
   `Advanced` ثم `Go to … (unsafe)`. هو سكربتك أنت على جدولك أنت.)
5. انسخ **رابط تطبيق الويب** — ينتهي بـ `/exec`.

> **مهم:** «أي شخص» تعني أن أيّ ضيف يستطيع الكتابة دون تسجيل دخول — وهذا
> المطلوب. لا تعني أن أحداً يستطيع رؤية جدولك؛ الجدول يبقى خاصاً بك.

### ٣ · الصق الرابط في الدعوة

في [`index.html`](index.html) ابحث عن `GUESTBOOK_URL` وضع الرابط بين علامتَي
التنصيص:

```js
const GUESTBOOK_URL = 'https://script.google.com/macros/s/AKfy…/exec'
```

ادفع التعديل، وتظهر صفحة **دفتر المباركات** خامسةً في الدعوة.

---

## ما دام الرابط فارغاً

المشهد **يحذف نفسه عند الإقلاع** وتعود الدعوة أربع صفحات، بلا أثر ولا خطأ —
تماماً كما يختفي أي حقل غير مملوء في هذا المشروع.

## الإشراف على المباركات

كل مباركة سطرٌ في الجدول: التاريخ، الاسم، النصّ، والحالة.

| ما تريده | ما تفعله |
| --- | --- |
| إخفاء مباركة مع الاحتفاظ بها | اكتب `hidden` في العمود الرابع |
| حذفها نهائياً | احذف الصفّ |
| قراءة الكل | الجدول نفسه هو السجلّ |

## ما هو مبنيّ في الحماية

- **لا HTML أبداً.** كل مباركة تُكتب بـ `textContent`، فلا يستطيع أحد وضع
  وسم أو سكربت أو رابط على شاشة غيره. مُختبَر بحقن `<script>` و
  `<img onerror>` — يظهران نصّاً.
- **مصيدة للبوتات**: حقل لا يراه إنسان؛ ما يملؤه بوت يُبتلع بهدوء.
- **مهلة ٤٥ ثانية** بين مباركة وأخرى من الجهاز نفسه.
- **حدود الطول**: ٤٠ حرفاً للاسم، ٢٤٠ للمباركة — في الواجهة وفي الخادم معاً.
- قفل كتابة على الخادم، فمباركتان تصلان معاً لا تدهسان بعضهما.

## الحصص المجانية

حصّة Apps Script المجانية ‎٢٠٬٠٠٠ استدعاء في اليوم. دعوة زفاف لا تقترب من
ذلك ولو بعُشره.
