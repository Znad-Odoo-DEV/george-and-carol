# دعوة زفاف جورج و كارول — المواصفة الكاملة

وثيقة مرجعية تصف كل ما في الدعوة: المحتوى، التصميم، الحركة، الصوت، والقرارات
التقنية. مكتوبة لتكون كافية لإعادة بناء العمل من الصفر.

**الرابط الحيّ:** https://znad-odoo-dev.github.io/george-and-carol/

---

## ١ · ما هو

دعوة زفاف رقمية، **صفحة HTML واحدة مكتفية بذاتها** — لا إطار عمل، لا خطوة بناء،
لا خادم خلفي. ملف `index.html` واحد (‎~٢٢٢ kB) يحمل كل شيء: البنية، التنسيق،
الحركة، والمنطق. مجلد `assets/` بجانبه للصور والصوت.

- **اللغة:** عربية بالكامل، اتجاه `rtl`
- **الجوال أولاً**، ثم تخطيط بطاقة مركزية على الشاشات العريضة
- **الاستضافة:** GitHub Pages (موقع ثابت)
- **الوزن الكلي للأصول:** ‎~٢٫٠ MB، منها ‎~١٫٣ MB فقط في التحميل الأول

---

## ٢ · التجربة، مشهداً بمشهد

خمسة مشاهد، **كلها مركّبة دائماً في DOM** ومكدّسة فوق بعضها، يُبدَّل بينها
بتغيير خاصية `data-active`. لا تُزال أي منها أثناء الانتقال — الإزالة تسبّب
وميضاً.

### المشهد ٠ — الباب

باب كنيسة **من صورة فوتوغرافية حقيقية**، بضلفتين خشبيتين مقوّستين داخل قوس من
الطوب. مغلق، معتم، وعليه:

- ختم دائري ذهبي منقوش `G ♡ C` داخل إكليل يُرسم خطاً بخط
- الاسمان: **جورج** و **كارول**
- تلميح: «اضغط لفتح الدعوة»

**عند الضغط في أي مكان على الباب:**

1. جرس كنيسة يرنّ (١٢ ثانية)
2. الضلفتان تنفتحان للداخل بمنظور ثلاثي الأبعاد
3. شعاع ضوء يتسع بينهما من خيط رفيع إلى الفتحة كاملة
4. **خمس حمامات حقيقية** — صورتان فوتوغرافيتان لحمامتين بيضاوين، مقصوصتان
   من خلفيتيهما بمفتاح لوني، في طورَي جناح متقابلين. كل واحدة تُستعمل
   بالاتجاهين، فخمسة طيور تعطي أربعة أشكال ولا يتكرّر طائر
5. تطير على **خمسة أقواس منفصلة**، بثلاث طبقات عمق: الخلفية صغيرة ضبابية،
   الوسط حادّ، والأمامية تعبر الكاميرا وتكبر لأكثر من ضعفين. كلها تبدأ
   معتمة داخل المدخل وتُضاء وهي تخرج للنور
6. **لا رفرفة مزيّفة**: الصورتان لطائرين مختلفين بزاويتَي تصوير مختلفتين،
   فتبديلهما يُقرأ كقفزة لا كرفرفة — والحركة الحقيقية هي الطيران نفسه
7. لمحة من مكان حقيقي خلف الباب (صورة رقصة العروسين، مغسولة بالضوء)
8. الغرفة كلها تتقدّم نحو المشاهد (`scale 2.3`) وتتلاشى في وميض أبيض
9. الموسيقى تبدأ من تحت ذيل الجرس

### المشهد ١ — الغلاف

صورة العروسين (يدان متشابكتان عند الغروب) **تذوب** في لوح كريمي — لا حافة
مقصوصة، بل تدرّج يتلاشى للأعلى. فوقه، من الأعلى للأسفل:

```
وَعَرَّفْتُهُمُ اسْمَكَ وَسَأُعَرِّفُهُمْ، لِيَكُونَ فِيهِمُ الْحُبُّ الَّذِي أَحْبَبْتَنِي بِهِ،
وَأَكُونَ أَنَا فِيهِمْ
(يوحنا ١٧: ٢٦)

رياض عوض        │        عون فيّاض
 وعائلته         │         وعائلته

يتشرفون بدعوتكم لحضور إكليل ولديهما

جورج  و  كارول
────────────

        الأحد
  ٢٠٢٦  │  ٢٠  │  أيلول
     السادسة مساءً
```

**كتلة التاريخ** مصفوفة كما في الدعوات المطبوعة: اسم اليوم فوق، الشهر والسنة
يحفّان رقماً كبيراً بالذهبي، الساعة تحت، وخطّان ذهبيان رفيعان فوق وتحت.

### المشهد ٢ — برنامج اليوم

**عدّاد تنازلي** يملأ عرض العمود: أربعة أرقام كبيرة بخط العنوان الذهبي، بينها
خطوط شعرية رفيعة، ومسطّر فوق وتحت. تحت كل رقم تسميته.

```
          باقٍ على فرحنا
   ───────────────────────────
   ٢٨   │   ٢٢   │   ٣١   │   ٥٢
   يوم    ساعة    دقيقة    ثانية
   ───────────────────────────
```

ثم **خط زمني** رأسي بشارات دائرية:

| الوقت | الحدث | المكان |
| --- | --- | --- |
| ٦:٠٠ مساءً | إكليل الزواج | كنيسة رقاد السيدة العذراء للروم الأرثوذكس — طرطوس |
| ٩:٠٠ مساءً | حفل العشاء | مطعم Barcod — Level 8 · زر «عرض موقع المطعم» |

وأسفل الصفحة صورة الرقصة الأولى، **مقوّسة من الأعلى** لتردّ على شكل الباب.

### المشهد ٣ — تأكيد الحضور

خلفية: صورة العناق، مرئية من الأعلى وتشتدّ عتامة الحجاب نحو الأسفل حيث النصّ.
إطار ذهبي يُرسم ضلعاً بضلع. المحتوى:

```
R S V P    (بحروف لاتينية كبيرة، متراكبة رأسياً)

للاعتذار قبل ٨ أيلول ٢٠٢٦

جورج · 0993988073
كارول · 0981330436

[ اعتذار من جورج — واتساب ]
[ اعتذار من كارول — واتساب ]

فرحنا تبدأ مع الربّ ونُكمّل بحضوركم
```

**رسالة الاعتذار الجاهزة** (تُفتح في واتساب على الرقم الصحيح):

> أهلاً جورج، نعتذر عن عدم تمكّننا من حضور حفلكما. ألف مبروك، وكل التوفيق لكما.
> عدد الأشخاص المعتذرين:

### المشهد ٤ — المباركات

```
باركوا العروسين

كلمة منكم تصل جورج وكارول مباشرةً.
أرسلوها كما هي، أو اكتبوها كما تحبّون

[ مباركة لجورج — واتساب ]
[ مباركة لكارول — واتساب ]

وفرحُنا يكتمل بكم
```

**رسالة المباركة الجاهزة:**

> مبروك يا جورج! ألف مبروك لك ولكارول — كل السعادة والبركة في حياتكما الجديدة.

وفي أسفل الصفحة **ذيل البطاقة المطبوعة**، مفصولاً بخط ذهبي — الملاحظة يميناً
والبنك يساراً، تماماً كما هو مطبوع:

| يمين | يسار |
| --- | --- |
| تم تنسيق الزهور مسبقاً<br>تُقبل التهاني في الكنيسة | بنك بيمو السعودي الفرنسي<br>`065013067600013011000` |

رقم الحساب **زر**: الضغط عليه ينسخه (٢١ رقماً لا يُكتب أحد يدوياً عن شاشة هاتف).

---

## ٣ · نظام التصميم

### الألوان

لوحة **شامبانيا عتيقة**، لا ذهب أصفر. `#c9a227` القديم كان أصفر يُقرأ على
الشاشة كقلم تحديد لا كذهب مطبوع على ورق.

```css
--ivory:          #f8f4ec    /* الأرضية */
--offwhite:       #efe8db
--kraft:          #d8c8a6
--gold:           #b89a5a    /* الخطوط والإطارات */
--gold-light:     #e3d2a4
--gold-shadow:    #8d7240    /* العناوين الذهبية */
--gold-deep:      #6f5a2f
--ink:            #2b2723    /* النصّ الأساسي */
--body:           #6f6254    /* نصّ ثانوي */
--muted:          #93866f    /* تسميات */
--envelope-black: #1a1614
```

والذهب الذي يحمل الصفحة — رقم التاريخ، العدّاد، تاريخ الاعتذار — ليس لوناً
مسطّحاً بل **تدرّج مقصوص على الحروف** (`--leaf`)، لأن الذهب الحقيقي على الورق
يتغيّر مع ميل الصفحة. تحته لون صلب، فإن لم يُدعم القصّ بقي الرقم ذهبياً.

### الخطوط

```css
--display: 'Aref Ruqaa', 'Amiri', Georgia, serif;   /* الأسماء والعناوين */
--serif:   'Amiri', 'Aref Ruqaa', Georgia, serif;   /* نصّ القراءة */
--sans:    'Almarai', system-ui, sans-serif;        /* التسميات والأرقام */
--latin:   'Cormorant Garamond', Georgia, serif;    /* اللاتيني */
--script:  'Great Vibes', cursive;
```

**قاعدة حاسمة:** العربية خطّ متّصل، لذا **لا يُستعمل `letter-spacing` على أي نصّ
عربي أبداً** — فهو يفكّ اتصال الحروف. يُستعمل `word-spacing` بدلاً منه
(`--air: 0.28em`). و`letter-spacing` محجوز للاتيني حصراً.

ولنفس السبب: **الأسماء تتحرك ككلمات كاملة، لا حرفاً حرفاً** — الكشف حرفاً بحرف
يعيد تشكيل الحرف الأخير في كل إطار فتهتزّ الكلمة.

### منحنيات الحركة

```css
--ease:       cubic-bezier(0.22, 0.61, 0.36, 1);
--ease-door:  cubic-bezier(0.5, 0, 0.15, 1);     /* ثقل الباب */
--ease-scene: cubic-bezier(0.4, 0, 0.2, 1);
--scene-ms:   700ms;
```

---

## ٤ · هندسة الباب

الباب **صورة واحدة** مقصوصة ثلاث قطع من نفس اللقطة (٣٦٠٠×٥٤٠٠):

- **الإطار** = الصورة كاملة، بمقياس ١٢٥٪ من المسرح
- **الضلفتان** = نصفها الأيمن والأيسر

داخل إطار الصورة بنسبة ٢:٣، تشغل الفتحة:

```
x من ٢٣٫٢٥٪ إلى ٧٠٫٥٠٪
y من ١٧٫٥٠٪ إلى ٧٧٫٠٠٪
القوس نصف دائرة تامّة = ٢٦٫٤٧٪ من ارتفاع الفتحة
```

كل ضلفة **مقصوصة على ربع القوس الخاص بها** (`clip-path: polygon()` بعشرين
نقطة)، فالطوب خارج المنحنى يبقى مع الجدار ولا يدور مع الخشب. ولأن الثلاثة من
نفس اللقطة وبنفس الهندسة، **الباب المغلق بلا أي خط فاصل**.

**الدوران:** `perspective: 780px` · `rotateY(∓74deg)` · مبدأ التحويل على المفصلة
(`left center` / `right center`) · مدة `1.9s` بمنحنى `--ease-door` وتأخير `.18s`.

---

## ٥ · الصوت

| | الجرس | الموسيقى |
| --- | --- | --- |
| الملف | `bell.mp3` — ١٤١ kB | `processional.mp3` — ٦٩١ kB |
| المواصفة | ١٢ ثانية، مونو | ٥٩ ثانية، ستيريو ٩٦ kbps |
| البدء | فور الضغط على الباب | **بعد ٦٫٥ ثانية** |
| المستوى | ٠٫٧ | ٠٫٥٥ |
| التلاشي | — | صعود على ٣٫٥ ثانية |

**لماذا ٦٫٥ ثانية:** الجرس يخفت في آخر خمس ثوانٍ منه. الموسيقى تطلع من تحت هذا
الذيل بدل أن تأتي بعد صمت — فتُقرأ كتسليم، لا كفجوة يبدو فيها الموقع معطّلاً.

**قفل التشغيل التلقائي:** المتصفحات ترفض صوتاً لم يطلبه أحد. لذا يُفتح قفل عنصر
الصوت **داخل لمسة فتح الباب نفسها** — يُشغَّل ثم يُوقَف ثم يُعاد للصفر — وبعدها
فقط يُجدوَل. وتلك اللمسة هي أيضاً ما يبدأ التنزيل: `preload="none"` حتى لا يقف
شيء بين الضيف والدعوة.

**زر إسكات** أسفل اليسار، الاختيار محفوظ في `localStorage`، والتبويب المخفي
يوقف التشغيل ويستأنف من مكانه.

---

## ٦ · التنقّل

- **تمرير للأسفل / سحب لأعلى / الأسهم / مسافة** → الصفحة التالية
- **نقر الثلث الأيسر** → للأمام · **الثلث الأيمن** → للخلف (RTL: الصفحة تُقرأ
  من اليمين لليسار، فالتنقّل يتبعها) · **الثلث الأوسط خامل** لتبقى الروابط قابلة
  للضغط
- **نقاط تقدّم** أسفل الشاشة، قابلة للضغط
- **تلميح «مرّر للأسفل»** يظهر مرة واحدة ثم لا يعود
- **رابط مباشر** `#scene-3` يفتح على تلك الصفحة ويتخطّى الباب
- الإدخال مقفول طوال الانتقال (٧٠٠ms + ١٠٠)
- نهاية المجموعة ترتدّ ارتداداً خفيفاً بدل أن تتجاهل السحبة

---

## ٧ · القرارات التقنية اللافتة

**التواريخ الزرقاء:** iOS يكتشف أي نصّ يشبه تاريخاً أو وقتاً أو رقماً طويلاً
ويحوّله لرابط بلونه الأزرق وخطّ تحته. الحلّ:

```html
<meta name="format-detection" content="telephone=no, date=no, address=no, email=no" />
```

**العدّ التنازلي** مثبّت على لحظة بعينها لا على ساعة القارئ:

```js
Date.parse('2026-09-20T18:00:00+03:00')   // سوريا ألغت التوقيت الصيفي ٢٠٢٢
```

فيرى ضيف في طرطوس وضيف في الخارج نفس الرقم. الأرقام تُعرض بالخانات
العربية-الهندية.

**متغيّر CSS غير معرّف يُبطل الاختصار كله** الذي يحتويه — `transition: transform
2.2s var(--ease-door)` بلا تعريف `--ease-door` يعني **لا انتقال إطلاقاً**، فيقفز
العنصر لحالته النهائية.

**`margin: … auto` على المحور العرضي لعنصر flex يلغي التمدّد** — العدّاد انكمش
إلى ١٢٨px داخل عمود عرضه ٣٣٨px حتى أُعطي `width: 100%` صراحةً.

**`backdrop-filter` يطمس مستطيل العنصر كاملاً** — فمهما نعّمت التدرّج فوقه،
الطمس نفسه يرسم خطاً حادّاً عبر الصفحة. لذا حُذف من لوح الغلاف ليذوب فعلاً.

**الأرقام القديمة الطراز:** Cormorant يرسم الأرقام بالنمط القديم (الصفر صغير
والستة تعلو) — جميل في تاريخ، خطأ في رقم حساب مصرفي يطابقه أحدهم مع بنكه.

---

## ٨ · الأصول

| الملف | الحجم | الاستعمال |
| --- | --- | --- |
| `door-frame.jpg` | ٢٧٦ kB | إطار الباب |
| `door-left.jpg` / `door-right.jpg` | ١٩١ / ١٨٧ kB | الضلفتان |
| `cover.jpg` | ٧١ kB | الغلاف — يدان عند الغروب |
| `finale.jpg` | ١٤٨ kB | صفحة الحضور — العناق |
| `dance.jpg` | ١٤٥ kB | خلف الباب + صفحة البرنامج |
| `gypsophila.jpg` · `sprigs.jpg` · `rose-bg.jpg` | ٤٥ / ١٠٢ / ٤٥ kB | زخارف |
| `bell.mp3` | ١٤١ kB | جرس الكنيسة |
| `processional.mp3` | ٦٩١ kB | زفّة الدخول |

كل الزخارف الأخرى — الإكليل، الشارات، الخطوط، الحمام، الزهور على القوس —
**مرسومة SVG أصلية**، لا صور.

---

## ٩ · إتاحة الوصول

- HTML دلالي، تنقّل كامل بلوحة المفاتيح، تسميات ARIA، منطقة `aria-live` تُعلن
  اسم كل صفحة
- **`prefers-reduced-motion`** مدعوم بالكامل: تتوقف كل الحركة، ولا يطير الحمام
  أمام من طلب السكون
- أهداف لمس لا تقل عن ٤٤px
- `100svh` بدل `100vh`، واحترام `env(safe-area-inset-*)`
- منع أي تمرير أفقي (مقيس: صفر على الجوال واللابتوب)
- أي ملف صورة أو صوت مفقود يختفي بهدوء ولا يكسر شيئاً

---

## ١٠ · معلومات الزفاف

| | |
| --- | --- |
| العروسان | جورج و كارول |
| العائلتان | رياض عوض وعائلته · عون فيّاض وعائلته |
| التاريخ | الأحد ٢٠ أيلول ٢٠٢٦ |
| الإكليل | ٦:٠٠ مساءً — كنيسة رقاد السيدة العذراء للروم الأرثوذكس، طرطوس |
| العشاء | ٩:٠٠ مساءً — مطعم Barcod، Level 8 |
| الآية | يوحنا ١٧: ٢٦ |
| للاعتذار | قبل ٨ أيلول ٢٠٢٦ |
| الهواتف | جورج 0993988073 · كارول 0981330436 |
| البنك | بيمو السعودي الفرنسي — 065013067600013011000 |
